#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../request-api/bin/request-api" "$@"
  ret=$?
else 
  node  "$basedir/../request-api/bin/request-api" "$@"
  ret=$?
fi
exit $ret
